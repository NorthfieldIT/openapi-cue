{
  "openapi": "3.0.0",
  "info": {
    "title": "Generated by cue.",
    "version": "no version"
  },
  "paths": {},
  "components": {
    "schemas": {
      "backend": {
        "description": "the backend is...",
        "type": "object",
        "required": [
          "host",
          "port",
          "type"
        ],
        "properties": {
          "host": {
            "description": "The host is",
            "type": "integer"
          },
          "port": {
            "description": "Port of where host is listening",
            "type": "integer"
          },
          "type": {
            "description": "Currently ELB or empty",
            "type": "string",
            "enum": [
              "",
              "ELB"
            ]
          }
        }
      },
      "cache": {
        "type": "object",
        "required": [
          "mode"
        ],
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "pass",
              "static"
            ]
          }
        }
      },
      "fallback": {
        "type": "object",
        "required": [
          "mode",
          "status"
        ],
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "static",
              "dynamic"
            ]
          },
          "status": {
            "type": "integer"
          }
        }
      },
      "rules": {
        "type": "object",
        "required": [
          "name",
          "path_regex",
          "method",
          "cache"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "path_regex": {
            "type": "string"
          },
          "method": {
            "type": "string",
            "enum": [
              "GET",
              "PUT",
              "POST",
              "OPTIONS"
            ]
          },
          "cache": {
            "$ref": "#/components/schemas/cache"
          },
          "fallback": {
            "type": "object",
            "$ref": "#/components/schemas/fallback"
          }
        }
      },
      "service": {
        "type": "object",
        "required": [
          "name",
          "hostname_reg",
          "backend",
          "rules"
        ],
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z]+$"
          },
          "hostname_reg": {
            "type": "string"
          },
          "backend": {
            "type": "object",
            "$ref": "#/components/schemas/backend"
          },
          "rules": {
            "type": "array",
            "items": {
              "type": "object",
              "$ref": "#/components/schemas/rules"
            }
          }
        }
      }
    }
  }
}